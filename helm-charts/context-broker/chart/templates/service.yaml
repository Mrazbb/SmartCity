apiVersion: v1
kind: Service
metadata:
  name: {{ include "context-broker.fullname" . }}
  labels:
    {{- include "context-broker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "context-broker.selectorLabels" . | nindent 4 }}